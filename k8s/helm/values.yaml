# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: eu.gcr.io/census-eq-ci/eq-address-lookup-api
  tag: latest
  pullPolicy: Always

lookup_url: ""

service:
  type: NodePort
  port: 80

ingress:
  enabled: true

resources:
  cpu: "3"
